- name: to print variables
  hosts: local
  connection: local
  vars_file:
   -lists.yaml
  tasks:
  - name: prinitng variables
    ansible.builtin.debug:
     msg: "name is {{ name }}, college name is {{ clg }}, cgpa is{{ cgpa }}" 