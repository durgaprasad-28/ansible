- name: packages install
  hosts: local
  connection: local
  tasks: 
  - name: loop install
    ansible.builtin.package:
     name: "{{ item }}"
     state: present
    loop:
    - nginx
    - mysql 